Model extracted and submitted by t4ils using klonoa2-tools (https://github.com/evilarceus/klonoa2-tools). No credit required.
Notes:
- If you experience textures seams/holes or colors blending in certain parts of the model, try switching the texture filtering mode in your renderer (linear -> nearest).
- LOD models are denoted by "High", "Medium", and "Low".
- Inflated models (e.g. for enemies) are denoted by "Inflated".
- Animated models are denoted by "Animated".
- Certain models may have morphs/shape keys, such as for facial expressions.
- Certain models may have multiple textures, such as for different model colors or slight changes to the character's face.